#include <iostream>                 //header file library
using namespace std;                //means we can use names for objects and variables from the standard library
                                    //blacnk line for better readability
int main() {                        //this is a function
    cout << "Hello World!";         //object used together with the insertion operator to output/print text
    return 0;                       //ends the main function
}                                   //dont forget to add closing curly brackt to actually end the function